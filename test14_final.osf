fun add($x, $y) { return $x + $y }

@items = ("one", "two", "three")
%map = ()
$idx = 0
while($idx < len(@items)) {
    $item = @items[$idx]
    hadd(%map, $idx, $item)
    inc $idx
}
$x = %map[0]
print($x)
print(haskeys(%map))
print("done")
